# REQUIRED
GEMINI_API_KEY=put-your-gemini-key-here

# OPTIONAL TUNING
DB_PATH=qc.db
TICK_SECONDS=1
WINDOW_SECONDS=1800        # 30 min rolling window
RAMP_LIMIT_PCT=0.5         # per step change limit for rawmix %
SEP_RAMP_LIMIT=3           # rpm per step
GYPSUM_RAMP_LIMIT=0.3      # % per step
LSF_MIN=98
LSF_MAX=102
BLAINE_MIN=320
BLAINE_MAX=360
FCAO_MAX=1.0
